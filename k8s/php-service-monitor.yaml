apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: phpservice-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: phpservice
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
